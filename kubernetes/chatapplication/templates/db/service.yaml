apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-mongo

spec:
  clusterIP: None   # 🔥 IMPORTANT (headless)

  selector:
    app: mongodb

  ports:
    - port: 27017
      targetPort: 27017