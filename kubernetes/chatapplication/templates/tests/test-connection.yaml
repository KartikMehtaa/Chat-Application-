apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "chatapplication.fullname" . }}-test-connection"
  labels:
    {{- include "chatapplication.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "chatapplication.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
